<nav class="nav-blue">
  <div class="navbar">
    <button class="btn btn-link d-md-none rounded-circle me-3" id="sidebarToggleTop" type="button"><i class="fas fa-bars"></i></button>
    <a class="navbar-brand " href="#">STUDY BUDDY</a>
    <div>
      <div class="d-inline m-md-3">
        <%= link_to new_note_path do%>
          <i class="fas fa-pencil-square"></i>
          <span class="text-white  d-none d-md-inline">New Note</span>
        <% end %>
      </div>
      <div class="d-inline m-md-3">
        <%= link_to new_study_session_path  do%>
          <i class="fas fa-table"></i>
          <span class="text-white  d-none d-md-inline">New Session</span>
        <% end %>
      </div>
      <div class="d-inline m-md-3">
        <%= link_to new_collection_path do%>
          <i class="fas fa-th-large"></i>
          <span class="text-white  d-none d-md-inline">New Collection</span>
        <% end %>
      </div>
    </div>
  </div>
  <% if sessions_page? %>
    <div class="justify-content-end navbar">
      <%=  link_to 'Most recent', url_for( :recent => :true ),class:"text-white btn"%>
      <%=  link_to 'Most ancient', url_for( :recent => :false ),class:"text-white btn"%>
    </div>
  <% end %>
</nav>
